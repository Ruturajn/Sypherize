! This is a variable declaration, with initialization.

int: a;

a := 39

int: z;

z := 99

` This is a function call.
def_func bar(int: a) ~ int {
    ! Do something here.
    ! func : foo := [int (int: w) { w }]
    ! foo
    a := a + 10 * 3
    a
}

2 * 3 / 2 + 1

bar(a)
a := bar(a);
