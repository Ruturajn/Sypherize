# The return value from the last expression is the exit code
# of the program.

# For more information about functions and the language
# see `factorial.sy`.

# This is a variable declaration, without initialization.
int: a;

a := 39

int: z;

z := 99

` This is a function call.
int: bar(int: a) := int: (int: a) {
    # Do something here.
    # func : foo := [int (int: w) { w }]
    # foo
    int: test := 10;
    int: xyz;
    a := a + 10 * 3
    a
}

2 * 3 / 2 + 1

bar(a)
a := bar(a);
